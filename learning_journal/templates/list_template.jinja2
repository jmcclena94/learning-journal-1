{% extends "base.jinja2" %}
{% block content %}
    {% for entry in entries %}
    <a href="{{ request.route_url("detail", id = entry.id) }}">{{ entry.title }}</a>
    <h4>{{ entry.created }}</h4>
    {% endfor %}
{% endblock content %}
