{% extends "base.jinja2" %}
{% block content %}
    {% for entry in entries %}
    <a href="/entry/{{ entry.id }}"><h2>{{ entry.title }}</h2></a>
    <h4>{{ entry.created }}</h4>
    {% endfor %}
{% endblock content %}
